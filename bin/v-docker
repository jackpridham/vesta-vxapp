#!/usr/bin/env bash
# info: access owner-scoped Docker project commands
# options: OPERATION [ARGUMENTS]
set -Eeuo pipefail

exec /usr/bin/sudo -n -- /usr/local/vesta/bin/v-run-user-docker-command "$@"
